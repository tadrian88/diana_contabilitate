// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"diana-contabilitate/backend/ent/accountingclient"
	"diana-contabilitate/backend/ent/contractextractionattempt"
	"diana-contabilitate/backend/ent/contractsourcedocument"
	"diana-contabilitate/backend/ent/predicate"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ContractSourceDocumentQuery is the builder for querying ContractSourceDocument entities.
type ContractSourceDocumentQuery struct {
	config
	ctx                    *QueryContext
	order                  []contractsourcedocument.OrderOption
	inters                 []Interceptor
	predicates             []predicate.ContractSourceDocument
	withClient             *AccountingClientQuery
	withExtractionAttempts *ContractExtractionAttemptQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ContractSourceDocumentQuery builder.
func (_q *ContractSourceDocumentQuery) Where(ps ...predicate.ContractSourceDocument) *ContractSourceDocumentQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *ContractSourceDocumentQuery) Limit(limit int) *ContractSourceDocumentQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *ContractSourceDocumentQuery) Offset(offset int) *ContractSourceDocumentQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *ContractSourceDocumentQuery) Unique(unique bool) *ContractSourceDocumentQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *ContractSourceDocumentQuery) Order(o ...contractsourcedocument.OrderOption) *ContractSourceDocumentQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryClient chains the current query on the "client" edge.
func (_q *ContractSourceDocumentQuery) QueryClient() *AccountingClientQuery {
	query := (&AccountingClientClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(contractsourcedocument.Table, contractsourcedocument.FieldID, selector),
			sqlgraph.To(accountingclient.Table, accountingclient.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, contractsourcedocument.ClientTable, contractsourcedocument.ClientColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryExtractionAttempts chains the current query on the "extraction_attempts" edge.
func (_q *ContractSourceDocumentQuery) QueryExtractionAttempts() *ContractExtractionAttemptQuery {
	query := (&ContractExtractionAttemptClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(contractsourcedocument.Table, contractsourcedocument.FieldID, selector),
			sqlgraph.To(contractextractionattempt.Table, contractextractionattempt.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, contractsourcedocument.ExtractionAttemptsTable, contractsourcedocument.ExtractionAttemptsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ContractSourceDocument entity from the query.
// Returns a *NotFoundError when no ContractSourceDocument was found.
func (_q *ContractSourceDocumentQuery) First(ctx context.Context) (*ContractSourceDocument, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{contractsourcedocument.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *ContractSourceDocumentQuery) FirstX(ctx context.Context) *ContractSourceDocument {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ContractSourceDocument ID from the query.
// Returns a *NotFoundError when no ContractSourceDocument ID was found.
func (_q *ContractSourceDocumentQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{contractsourcedocument.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *ContractSourceDocumentQuery) FirstIDX(ctx context.Context) string {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ContractSourceDocument entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ContractSourceDocument entity is found.
// Returns a *NotFoundError when no ContractSourceDocument entities are found.
func (_q *ContractSourceDocumentQuery) Only(ctx context.Context) (*ContractSourceDocument, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{contractsourcedocument.Label}
	default:
		return nil, &NotSingularError{contractsourcedocument.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *ContractSourceDocumentQuery) OnlyX(ctx context.Context) *ContractSourceDocument {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ContractSourceDocument ID in the query.
// Returns a *NotSingularError when more than one ContractSourceDocument ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *ContractSourceDocumentQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{contractsourcedocument.Label}
	default:
		err = &NotSingularError{contractsourcedocument.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *ContractSourceDocumentQuery) OnlyIDX(ctx context.Context) string {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ContractSourceDocuments.
func (_q *ContractSourceDocumentQuery) All(ctx context.Context) ([]*ContractSourceDocument, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ContractSourceDocument, *ContractSourceDocumentQuery]()
	return withInterceptors[[]*ContractSourceDocument](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *ContractSourceDocumentQuery) AllX(ctx context.Context) []*ContractSourceDocument {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ContractSourceDocument IDs.
func (_q *ContractSourceDocumentQuery) IDs(ctx context.Context) (ids []string, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(contractsourcedocument.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *ContractSourceDocumentQuery) IDsX(ctx context.Context) []string {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *ContractSourceDocumentQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*ContractSourceDocumentQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *ContractSourceDocumentQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *ContractSourceDocumentQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *ContractSourceDocumentQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ContractSourceDocumentQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *ContractSourceDocumentQuery) Clone() *ContractSourceDocumentQuery {
	if _q == nil {
		return nil
	}
	return &ContractSourceDocumentQuery{
		config:                 _q.config,
		ctx:                    _q.ctx.Clone(),
		order:                  append([]contractsourcedocument.OrderOption{}, _q.order...),
		inters:                 append([]Interceptor{}, _q.inters...),
		predicates:             append([]predicate.ContractSourceDocument{}, _q.predicates...),
		withClient:             _q.withClient.Clone(),
		withExtractionAttempts: _q.withExtractionAttempts.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithClient tells the query-builder to eager-load the nodes that are connected to
// the "client" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ContractSourceDocumentQuery) WithClient(opts ...func(*AccountingClientQuery)) *ContractSourceDocumentQuery {
	query := (&AccountingClientClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withClient = query
	return _q
}

// WithExtractionAttempts tells the query-builder to eager-load the nodes that are connected to
// the "extraction_attempts" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ContractSourceDocumentQuery) WithExtractionAttempts(opts ...func(*ContractExtractionAttemptQuery)) *ContractSourceDocumentQuery {
	query := (&ContractExtractionAttemptClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withExtractionAttempts = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ClientID string `json:"client_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ContractSourceDocument.Query().
//		GroupBy(contractsourcedocument.FieldClientID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *ContractSourceDocumentQuery) GroupBy(field string, fields ...string) *ContractSourceDocumentGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ContractSourceDocumentGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = contractsourcedocument.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ClientID string `json:"client_id,omitempty"`
//	}
//
//	client.ContractSourceDocument.Query().
//		Select(contractsourcedocument.FieldClientID).
//		Scan(ctx, &v)
func (_q *ContractSourceDocumentQuery) Select(fields ...string) *ContractSourceDocumentSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &ContractSourceDocumentSelect{ContractSourceDocumentQuery: _q}
	sbuild.label = contractsourcedocument.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ContractSourceDocumentSelect configured with the given aggregations.
func (_q *ContractSourceDocumentQuery) Aggregate(fns ...AggregateFunc) *ContractSourceDocumentSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *ContractSourceDocumentQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !contractsourcedocument.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *ContractSourceDocumentQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ContractSourceDocument, error) {
	var (
		nodes       = []*ContractSourceDocument{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withClient != nil,
			_q.withExtractionAttempts != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ContractSourceDocument).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ContractSourceDocument{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withClient; query != nil {
		if err := _q.loadClient(ctx, query, nodes, nil,
			func(n *ContractSourceDocument, e *AccountingClient) { n.Edges.Client = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withExtractionAttempts; query != nil {
		if err := _q.loadExtractionAttempts(ctx, query, nodes,
			func(n *ContractSourceDocument) { n.Edges.ExtractionAttempts = []*ContractExtractionAttempt{} },
			func(n *ContractSourceDocument, e *ContractExtractionAttempt) {
				n.Edges.ExtractionAttempts = append(n.Edges.ExtractionAttempts, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *ContractSourceDocumentQuery) loadClient(ctx context.Context, query *AccountingClientQuery, nodes []*ContractSourceDocument, init func(*ContractSourceDocument), assign func(*ContractSourceDocument, *AccountingClient)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*ContractSourceDocument)
	for i := range nodes {
		fk := nodes[i].ClientID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(accountingclient.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "client_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ContractSourceDocumentQuery) loadExtractionAttempts(ctx context.Context, query *ContractExtractionAttemptQuery, nodes []*ContractSourceDocument, init func(*ContractSourceDocument), assign func(*ContractSourceDocument, *ContractExtractionAttempt)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*ContractSourceDocument)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(contractextractionattempt.FieldDocumentID)
	}
	query.Where(predicate.ContractExtractionAttempt(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(contractsourcedocument.ExtractionAttemptsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.DocumentID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "document_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *ContractSourceDocumentQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *ContractSourceDocumentQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(contractsourcedocument.Table, contractsourcedocument.Columns, sqlgraph.NewFieldSpec(contractsourcedocument.FieldID, field.TypeString))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, contractsourcedocument.FieldID)
		for i := range fields {
			if fields[i] != contractsourcedocument.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withClient != nil {
			_spec.Node.AddColumnOnce(contractsourcedocument.FieldClientID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *ContractSourceDocumentQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(contractsourcedocument.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = contractsourcedocument.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ContractSourceDocumentGroupBy is the group-by builder for ContractSourceDocument entities.
type ContractSourceDocumentGroupBy struct {
	selector
	build *ContractSourceDocumentQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *ContractSourceDocumentGroupBy) Aggregate(fns ...AggregateFunc) *ContractSourceDocumentGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *ContractSourceDocumentGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ContractSourceDocumentQuery, *ContractSourceDocumentGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *ContractSourceDocumentGroupBy) sqlScan(ctx context.Context, root *ContractSourceDocumentQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ContractSourceDocumentSelect is the builder for selecting fields of ContractSourceDocument entities.
type ContractSourceDocumentSelect struct {
	*ContractSourceDocumentQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *ContractSourceDocumentSelect) Aggregate(fns ...AggregateFunc) *ContractSourceDocumentSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *ContractSourceDocumentSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ContractSourceDocumentQuery, *ContractSourceDocumentSelect](ctx, _s.ContractSourceDocumentQuery, _s, _s.inters, v)
}

func (_s *ContractSourceDocumentSelect) sqlScan(ctx context.Context, root *ContractSourceDocumentQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
