// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"diana-contabilitate/backend/ent/accountingclient"
	"diana-contabilitate/backend/ent/classificationrule"
	"diana-contabilitate/backend/ent/predicate"
	"diana-contabilitate/backend/ent/ruleversion"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ClassificationRuleQuery is the builder for querying ClassificationRule entities.
type ClassificationRuleQuery struct {
	config
	ctx           *QueryContext
	order         []classificationrule.OrderOption
	inters        []Interceptor
	predicates    []predicate.ClassificationRule
	withClient    *AccountingClientQuery
	withParent    *ClassificationRuleQuery
	withOverrides *ClassificationRuleQuery
	withVersions  *RuleVersionQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ClassificationRuleQuery builder.
func (_q *ClassificationRuleQuery) Where(ps ...predicate.ClassificationRule) *ClassificationRuleQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *ClassificationRuleQuery) Limit(limit int) *ClassificationRuleQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *ClassificationRuleQuery) Offset(offset int) *ClassificationRuleQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *ClassificationRuleQuery) Unique(unique bool) *ClassificationRuleQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *ClassificationRuleQuery) Order(o ...classificationrule.OrderOption) *ClassificationRuleQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryClient chains the current query on the "client" edge.
func (_q *ClassificationRuleQuery) QueryClient() *AccountingClientQuery {
	query := (&AccountingClientClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(classificationrule.Table, classificationrule.FieldID, selector),
			sqlgraph.To(accountingclient.Table, accountingclient.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, classificationrule.ClientTable, classificationrule.ClientColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryParent chains the current query on the "parent" edge.
func (_q *ClassificationRuleQuery) QueryParent() *ClassificationRuleQuery {
	query := (&ClassificationRuleClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(classificationrule.Table, classificationrule.FieldID, selector),
			sqlgraph.To(classificationrule.Table, classificationrule.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, classificationrule.ParentTable, classificationrule.ParentColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOverrides chains the current query on the "overrides" edge.
func (_q *ClassificationRuleQuery) QueryOverrides() *ClassificationRuleQuery {
	query := (&ClassificationRuleClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(classificationrule.Table, classificationrule.FieldID, selector),
			sqlgraph.To(classificationrule.Table, classificationrule.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, classificationrule.OverridesTable, classificationrule.OverridesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryVersions chains the current query on the "versions" edge.
func (_q *ClassificationRuleQuery) QueryVersions() *RuleVersionQuery {
	query := (&RuleVersionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(classificationrule.Table, classificationrule.FieldID, selector),
			sqlgraph.To(ruleversion.Table, ruleversion.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, classificationrule.VersionsTable, classificationrule.VersionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ClassificationRule entity from the query.
// Returns a *NotFoundError when no ClassificationRule was found.
func (_q *ClassificationRuleQuery) First(ctx context.Context) (*ClassificationRule, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{classificationrule.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *ClassificationRuleQuery) FirstX(ctx context.Context) *ClassificationRule {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ClassificationRule ID from the query.
// Returns a *NotFoundError when no ClassificationRule ID was found.
func (_q *ClassificationRuleQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{classificationrule.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *ClassificationRuleQuery) FirstIDX(ctx context.Context) string {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ClassificationRule entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ClassificationRule entity is found.
// Returns a *NotFoundError when no ClassificationRule entities are found.
func (_q *ClassificationRuleQuery) Only(ctx context.Context) (*ClassificationRule, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{classificationrule.Label}
	default:
		return nil, &NotSingularError{classificationrule.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *ClassificationRuleQuery) OnlyX(ctx context.Context) *ClassificationRule {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ClassificationRule ID in the query.
// Returns a *NotSingularError when more than one ClassificationRule ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *ClassificationRuleQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{classificationrule.Label}
	default:
		err = &NotSingularError{classificationrule.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *ClassificationRuleQuery) OnlyIDX(ctx context.Context) string {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ClassificationRules.
func (_q *ClassificationRuleQuery) All(ctx context.Context) ([]*ClassificationRule, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ClassificationRule, *ClassificationRuleQuery]()
	return withInterceptors[[]*ClassificationRule](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *ClassificationRuleQuery) AllX(ctx context.Context) []*ClassificationRule {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ClassificationRule IDs.
func (_q *ClassificationRuleQuery) IDs(ctx context.Context) (ids []string, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(classificationrule.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *ClassificationRuleQuery) IDsX(ctx context.Context) []string {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *ClassificationRuleQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*ClassificationRuleQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *ClassificationRuleQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *ClassificationRuleQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *ClassificationRuleQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ClassificationRuleQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *ClassificationRuleQuery) Clone() *ClassificationRuleQuery {
	if _q == nil {
		return nil
	}
	return &ClassificationRuleQuery{
		config:        _q.config,
		ctx:           _q.ctx.Clone(),
		order:         append([]classificationrule.OrderOption{}, _q.order...),
		inters:        append([]Interceptor{}, _q.inters...),
		predicates:    append([]predicate.ClassificationRule{}, _q.predicates...),
		withClient:    _q.withClient.Clone(),
		withParent:    _q.withParent.Clone(),
		withOverrides: _q.withOverrides.Clone(),
		withVersions:  _q.withVersions.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithClient tells the query-builder to eager-load the nodes that are connected to
// the "client" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ClassificationRuleQuery) WithClient(opts ...func(*AccountingClientQuery)) *ClassificationRuleQuery {
	query := (&AccountingClientClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withClient = query
	return _q
}

// WithParent tells the query-builder to eager-load the nodes that are connected to
// the "parent" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ClassificationRuleQuery) WithParent(opts ...func(*ClassificationRuleQuery)) *ClassificationRuleQuery {
	query := (&ClassificationRuleClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withParent = query
	return _q
}

// WithOverrides tells the query-builder to eager-load the nodes that are connected to
// the "overrides" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ClassificationRuleQuery) WithOverrides(opts ...func(*ClassificationRuleQuery)) *ClassificationRuleQuery {
	query := (&ClassificationRuleClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOverrides = query
	return _q
}

// WithVersions tells the query-builder to eager-load the nodes that are connected to
// the "versions" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ClassificationRuleQuery) WithVersions(opts ...func(*RuleVersionQuery)) *ClassificationRuleQuery {
	query := (&RuleVersionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withVersions = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Reference string `json:"reference,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ClassificationRule.Query().
//		GroupBy(classificationrule.FieldReference).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *ClassificationRuleQuery) GroupBy(field string, fields ...string) *ClassificationRuleGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ClassificationRuleGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = classificationrule.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Reference string `json:"reference,omitempty"`
//	}
//
//	client.ClassificationRule.Query().
//		Select(classificationrule.FieldReference).
//		Scan(ctx, &v)
func (_q *ClassificationRuleQuery) Select(fields ...string) *ClassificationRuleSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &ClassificationRuleSelect{ClassificationRuleQuery: _q}
	sbuild.label = classificationrule.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ClassificationRuleSelect configured with the given aggregations.
func (_q *ClassificationRuleQuery) Aggregate(fns ...AggregateFunc) *ClassificationRuleSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *ClassificationRuleQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !classificationrule.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *ClassificationRuleQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ClassificationRule, error) {
	var (
		nodes       = []*ClassificationRule{}
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withClient != nil,
			_q.withParent != nil,
			_q.withOverrides != nil,
			_q.withVersions != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ClassificationRule).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ClassificationRule{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withClient; query != nil {
		if err := _q.loadClient(ctx, query, nodes, nil,
			func(n *ClassificationRule, e *AccountingClient) { n.Edges.Client = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withParent; query != nil {
		if err := _q.loadParent(ctx, query, nodes, nil,
			func(n *ClassificationRule, e *ClassificationRule) { n.Edges.Parent = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withOverrides; query != nil {
		if err := _q.loadOverrides(ctx, query, nodes,
			func(n *ClassificationRule) { n.Edges.Overrides = []*ClassificationRule{} },
			func(n *ClassificationRule, e *ClassificationRule) { n.Edges.Overrides = append(n.Edges.Overrides, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withVersions; query != nil {
		if err := _q.loadVersions(ctx, query, nodes,
			func(n *ClassificationRule) { n.Edges.Versions = []*RuleVersion{} },
			func(n *ClassificationRule, e *RuleVersion) { n.Edges.Versions = append(n.Edges.Versions, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *ClassificationRuleQuery) loadClient(ctx context.Context, query *AccountingClientQuery, nodes []*ClassificationRule, init func(*ClassificationRule), assign func(*ClassificationRule, *AccountingClient)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*ClassificationRule)
	for i := range nodes {
		if nodes[i].ClientID == nil {
			continue
		}
		fk := *nodes[i].ClientID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(accountingclient.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "client_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ClassificationRuleQuery) loadParent(ctx context.Context, query *ClassificationRuleQuery, nodes []*ClassificationRule, init func(*ClassificationRule), assign func(*ClassificationRule, *ClassificationRule)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*ClassificationRule)
	for i := range nodes {
		if nodes[i].ParentRuleID == nil {
			continue
		}
		fk := *nodes[i].ParentRuleID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(classificationrule.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "parent_rule_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ClassificationRuleQuery) loadOverrides(ctx context.Context, query *ClassificationRuleQuery, nodes []*ClassificationRule, init func(*ClassificationRule), assign func(*ClassificationRule, *ClassificationRule)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*ClassificationRule)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(classificationrule.FieldParentRuleID)
	}
	query.Where(predicate.ClassificationRule(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(classificationrule.OverridesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ParentRuleID
		if fk == nil {
			return fmt.Errorf(`foreign-key "parent_rule_id" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "parent_rule_id" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *ClassificationRuleQuery) loadVersions(ctx context.Context, query *RuleVersionQuery, nodes []*ClassificationRule, init func(*ClassificationRule), assign func(*ClassificationRule, *RuleVersion)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*ClassificationRule)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(ruleversion.FieldRuleID)
	}
	query.Where(predicate.RuleVersion(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(classificationrule.VersionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RuleID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "rule_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *ClassificationRuleQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *ClassificationRuleQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(classificationrule.Table, classificationrule.Columns, sqlgraph.NewFieldSpec(classificationrule.FieldID, field.TypeString))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, classificationrule.FieldID)
		for i := range fields {
			if fields[i] != classificationrule.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withClient != nil {
			_spec.Node.AddColumnOnce(classificationrule.FieldClientID)
		}
		if _q.withParent != nil {
			_spec.Node.AddColumnOnce(classificationrule.FieldParentRuleID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *ClassificationRuleQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(classificationrule.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = classificationrule.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ClassificationRuleGroupBy is the group-by builder for ClassificationRule entities.
type ClassificationRuleGroupBy struct {
	selector
	build *ClassificationRuleQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *ClassificationRuleGroupBy) Aggregate(fns ...AggregateFunc) *ClassificationRuleGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *ClassificationRuleGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ClassificationRuleQuery, *ClassificationRuleGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *ClassificationRuleGroupBy) sqlScan(ctx context.Context, root *ClassificationRuleQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ClassificationRuleSelect is the builder for selecting fields of ClassificationRule entities.
type ClassificationRuleSelect struct {
	*ClassificationRuleQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *ClassificationRuleSelect) Aggregate(fns ...AggregateFunc) *ClassificationRuleSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *ClassificationRuleSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ClassificationRuleQuery, *ClassificationRuleSelect](ctx, _s.ClassificationRuleQuery, _s, _s.inters, v)
}

func (_s *ClassificationRuleSelect) sqlScan(ctx context.Context, root *ClassificationRuleQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
